# ============================================================
# 前端环境变量示例 —— 复制为 .env 后按需修改
# ============================================================
# Vite 只会把以 VITE_ 开头的变量注入到客户端代码。
# 真实 .env 不要提交到 git，只提交本示例文件。

# 后端 API 的 base URL。
#   - 留空（默认）：走 vite dev server 的 /api 反向代理（dev 时推荐）
#   - 显式 URL（生产）：前端直接请求该地址，例如 https://api.example.com
#     注意：所有 api/*.ts 都拼接了 /api 前缀，所以 VITE_API_BASE 不要再带 /api。
VITE_API_BASE=
